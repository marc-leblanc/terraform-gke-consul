apiVersion: v1
kind: ConfigMap
metadata:
  name: consul-dashboard
  namespace: ${grafana_ns}
  labels:
    app: grafana
    grafana_dashboard: "1"
data:
  consul-dashboard.json: |-
    ${consul_dashboard_json}
